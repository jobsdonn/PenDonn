"""PenDonn modern web UI (FastAPI + HTMX + Tailwind).

Built parallel to the legacy Flask UI at web/. The legacy UI keeps running
on port 8080 until this one is feature-complete and the installer is
flipped over. See docs/WEBUI_MIGRATION.md (forthcoming) for the plan.
"""
