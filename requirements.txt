scapy==2.5.0
psutil==5.9.6
requests==2.31.0
pillow>=10.3.0
RPi.GPIO==0.7.1; platform_machine == "armv7l" or platform_machine == "aarch64"
spidev==3.6; platform_machine == "armv7l" or platform_machine == "aarch64"
gpiozero==2.0.1; platform_machine == "armv7l" or platform_machine == "aarch64"
python-nmap==0.7.1
paramiko==3.3.1
cryptography==41.0.0

# Plugin dependencies
dnspython==2.4.2
# pysnmp 4.x is broken on Python 3.12+ (uses removed `imp` module). snmp_scanner
# plugin will fail to load; all other plugins are unaffected. Tracked for future
# migration to pysnmp-lextudio (the maintained fork).
# pysnmp==4.4.12
reportlab==4.0.7

# Web UI (FastAPI + HTMX, port 8081)
fastapi>=0.110,<1.0
uvicorn[standard]>=0.30,<1.0
jinja2>=3.1
python-multipart>=0.0.9
itsdangerous>=2.1
werkzeug>=3.0  # generate_password_hash / check_password_hash for basic_auth
